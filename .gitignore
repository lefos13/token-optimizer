node_modules/
dist/
.test-build/
gateway/dist/
.codex-local-test-runs/
# Antigravity plugin output is generated; not tracked.
plugin/antigravity/
# Claude Code plugin is committed so the marketplace install (git-based) can copy it.
# Codex plugin is committed so the repo marketplace install can copy it.

.DS_Store
graphify-out/
