{
  "mcpServers": {
    "token_optimizer": {
      "command": "node",
      "args": [
        "C:\\Users\\slode\\.cursor\\token-optimizer-server\\start.js"
      ],
      "env": {
        "LLM_GATEWAY_URL": "${env:LLM_GATEWAY_URL}",
        "LLM_GATEWAY_TOKEN": "${env:LLM_GATEWAY_TOKEN}"
      }
    }
  }
}
